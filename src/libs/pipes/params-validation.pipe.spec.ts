import { ParamsValidationPipe } from './params-validation.pipe';

describe('ParamsValidationPipe', () => {
  it('should be defined', () => {
    expect(new ParamsValidationPipe()).toBeDefined();
  });
});
